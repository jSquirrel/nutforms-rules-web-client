"use strict";
import RuleWeaver from '../../src/rules/RuleWeaver'
// import '../../node_modules/nutforms-web-client/src/model'

Object.assign = require('object-assign');
let chai = require('chai'),
    expect = require('chai').expect,
    assert = require('chai').assert;

chai.should();

describe('RuleWeaver', function () {
    describe('#addObservers', function () { // integration test
        it('should add functions to fields', function () {
            // toDo: implement integration tests
        })
    });
});